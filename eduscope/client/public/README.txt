Drop your background video here as `bg-video.mp4`.

It will be played silently, muted, looping, on the landing page, behind a dark gradient overlay.

If the file is missing, the landing page still renders correctly with the solid dark background.
